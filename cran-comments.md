## Re-submission
This is a resubmission of the package (v0.99.1) to address CRAN check results and prevent archival.

## Fixes Applied
* Updated `DESCRIPTION` with `Authors@R` structured field.
* Corrected LaTeX formatting in `.Rd` documentation files.
* Synchronized `toy_data` documentation with `data/toy_data.rda`.
* Added missing `@importFrom` directives for `grDevices`, `graphics`, and `stats` base utility functions into `NAMESPACE`.
* Cleaned root directory non-standard files.

## Test Results
* `devtools::check()` yielded 0 Errors, 0 Warnings, and 0 Notes on local build.
