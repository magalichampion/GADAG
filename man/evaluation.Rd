% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{evaluation}
\alias{evaluation}
\title{Evaluate the fitness of a population}
\usage{
evaluation(Pop, X, XtX, lambda, Gradcontrol = list(tol.obj = 1e-06, max.ite =
  50), ncores = 1)
}
\arguments{
\item{Pop}{Population of permutations from [1,p] (output from create.population() function for example).}

\item{X}{Design matrix, with samples (n) in rows and variables (p) in columns.}

\item{XtX}{Cross-product of X. Should be computed (crossprod(X)) before running the evaluation() function.}

\item{lambda}{Parameter of penalization (>0).}

\item{Gradcontrol}{A list containing the parameters for controlling the inner optimization, i.e. the gradient descent
\itemize{
\item{tol.obj.inner}{ tolerance (>0),}
\item{max.ite.inner}{ maximum number of iterations (>0).}
}}

\item{ncores}{Number of cores (>1, depending on your computer).}
}
\value{
A list with the following elements:
\itemize{
\item{Tpop}{ Matrix with pxp columns, each column corresponding to the best triangular matrix (in a vector form) associated to each permutation of the population.}
\item{f}{ Fitness of the population.}
}
}
\description{
Internal function of the genetic algorithm that evaluates the fitness (penalized log-likelihood) of a population of permutations. It internally computes the best triangular matrix associated to each permutation.
}
\examples{
 ########################################################
 # Loading toy data
 ########################################################
 data(toy_data)

 ########################################################
 # Creating a population of permutations
 ########################################################
 Population <- create.population(p=ncol(toy_data$X),pop.size=20)

 ########################################################
 # Evaluating the fitness of the population
 ########################################################
 Evaluation <- evaluation(Pop=Population,X=toy_data$X,
                          XtX=crossprod(toy_data$X),lambda=0.1)
}
\author{
\packageAuthor{GADAG}
}
\seealso{
\code{\link{GADAG}}, \code{\link{GADAG_Run}}.
}

